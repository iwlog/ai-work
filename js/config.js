/**
 * line-life-log連携用の設定（Phase 4B）。
 *
 * API_BASE_URLだけをここで管理し、他のファイルには一切書かない（URLの重複記載を避ける）。
 * 環境（localhost / ngrok一時URL / 本番ドメイン）を切り替える場合は、このファイルの
 * API_BASE_URLの値だけを書き換えれば良い。認証情報は置かない（このAPIは認証不要の
 * 読み取り専用エンドポイントのため、そもそも書く必要がない）。
 *
 * ローカル確認時に、このファイルをコミット対象のまま一時URLで書き換えたくない場合は、
 * ページのURLに ?api=http://localhost:3000 のようにクエリパラメータを付けることで
 * 一時的に上書きできる（js/api-client.js参照。クエリパラメータはコミットされない）。
 *
 * 未設定（空文字のまま）の場合、記事一覧・詳細ページは「接続先が設定されていません」という
 * 分かりやすいエラーを表示する（js/articles.js参照）。
 */
window.LINE_LIFE_LOG_CONFIG = {
  API_BASE_URL: '',
};
